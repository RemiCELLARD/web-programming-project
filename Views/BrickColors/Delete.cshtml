@model Web_Programming_Project.Models.BrickColor

@{
    ViewData["Title"] = "Delete";
}

<h1>Delete</h1>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>BrickColor</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.BrickColorName)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.BrickColorName)
        </dd>
        <dt class = "col-sm-2">
            Color RGBA
        </dt>
        <dd class = "col-sm-10">
            <div class="minicolors minicolors-theme-bootstrap minicolors-with-opacity">
                <span class="minicolors-swatch minicolors-sprite minicolors-input-swatch">
                    <span class="minicolors-swatch-color" style="opacity: @(Model.BrickColorAlpha); background-color: rgb(@(Model.BrickColorRed), @(Model.BrickColorGreen), @(Model.BrickColorBlue));"></span>
                </span>
            </div>
        </dd>
    </dl>
    
    <form asp-action="Delete">
        <input type="hidden" asp-for="Id" />
        <input type="submit" value="Delete" class="btn btn-danger" /> |
        <a asp-action="Index">Back to List</a>
    </form>
</div>
