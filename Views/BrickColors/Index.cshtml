@model IEnumerable<Web_Programming_Project.Models.BrickColor>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.BrickColorName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.BrickColorRed)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.BrickColorGreen)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.BrickColorBlue)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.BrickColorAlpha)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.BrickColorName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.BrickColorRed)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.BrickColorGreen)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.BrickColorBlue)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.BrickColorAlpha)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
