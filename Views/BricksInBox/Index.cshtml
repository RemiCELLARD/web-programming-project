@model IEnumerable<Web_Programming_Project.Models.BrickInBox>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.BrickInBoxBox)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.BrickInBoxBrick)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.BrickInBoxQuantity)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.BrickInBoxBox.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.BrickInBoxBrick.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.BrickInBoxQuantity)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
